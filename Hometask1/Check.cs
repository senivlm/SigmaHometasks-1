namespace Hometask1;

public static class Check
{
    public static void GetProductInfo(Product product) => Console.WriteLine(product.ToString());
    public static void GetBuyInfo(Buy buy) => Console.WriteLine(buy.ToString());
}